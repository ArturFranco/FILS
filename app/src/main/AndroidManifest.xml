<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabriel.fils">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovaAtividade"
            android:label="@string/nova_atividade"/>
        <activity android:name=".Historico"
            android:label="Histórico"/>
        <activity android:name=".NovoTreino"
            android:label="Treinos"/>
        <activity android:name=".NovaRefeicao"
            android:label="Refeições"/>
        <activity android:name=".AddTreino"
            android:label="Adicionar Treino"/>
        <activity android:name="AddRefeicao"
            android:label="Adicionar Refeição"/>
        <activity android:name=".Agendamento"
            android:label="Agendamento"/>
        <activity android:name=".Consultas"
            android:label="Consultas Marcadas"/>
        <activity android:name=".NovaConsulta"
            android:label="Marcar nova consulta"/>
        <activity android:name=".LoginActivity"
            android:label=""/>
        <activity android:name=".PerfilActivity"
            android:label=""/>
        <activity android:name=".Metas"
            android:label="Metas"/>
        <activity android:name=".AddGrupo"
            android:label="Adicionar Grupo"/>
        <activity android:name=".CollectionDemoActivity" />
        <activity android:name=".ProfissionalMainActivity" />
        <activity
            android:name=".AlunoActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"/>


        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>


        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"

            android:label="@string/app_name" />


        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

    </application>
</manifest>
